export type Options = {
  url: string;
  name: string;
  git: boolean;
  install: string;
};

export type repoInfo = {
  owner: string;
  repo: string;
  sub: string;
  repoUrl: string;
  branch: string;
};
