{
  "name": "templates",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "test": "turbo test",
    "clean": "turbo clean",
    "root:format": "biome format . --write",
    "root:lint": "biome lint . --fix",
    "root:clean": "",
    "prepare": "husky",
    "build:prepare": "pnpm run lint && pnpm run format && pnpm run build",
    "dev:prepare": "git pull origin main && pnpm i"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "husky": "^9.0.11",
    "turbo": "^2.0.6"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.6.0"
}